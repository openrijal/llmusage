# Task List - PR #68: feat/gemini-cli-jsonl-support

## Session Context

- **Date**: 2026-04-16
- **Branch**: feat/gemini-cli-jsonl-support
- **PR**: #68 - Add Gemini CLI JSONL log support and expand .gitignore
- **Objective**: Implement JSONL session log parsing for Gemini CLI and expand .gitignore

## Tasks Completed

### 1. Gemini CLI JSONL Parser Implementation
- [x] Replace stub protobuf implementation with JSONL parser
- [x] Parse `~/.gemini/tmp/<project>/chats/**/*.jsonl` files
- [x] Handle message deduplication (messages written twice - without then with tokens)
- [x] Handle `$rewindTo` records that truncate session history
- [x] Handle subagent subdirectories under parent session IDs
- [x] Skip zero-token messages
- [x] Add unit tests for all parsing behaviors

### 2. Data Model Integration
- [x] Map JSONL fields to UsageRecord schema
- [x] Extract input, output, cached tokens from tokens summary
- [x] Store thoughts/tool tokens in metadata JSON blob
- [x] Calculate cost via costs::calculate_cost()

### 3. Legacy Protobuf Detection
- [x] Detect legacy `.pb` files in `~/.gemini/antigravity/`
- [x] Print upgrade warning when legacy files found but no JSONL sessions
- [x] Document that protobuf sessions are opaque and unparseable

### 4. Collector Auto-Detection Update
- [x] Update get_collectors() to detect `~/.gemini/tmp` or legacy antigravity dirs
- [x] Enable gemini_cli collector if either path exists

### 5. .gitignore Expansion
- [x] Add Rust artifacts (target/, *.rs.bk, *.pdb)
- [x] Add database files (*.db, *.db-wal, *.db-shm)
- [x] Add Playwright MCP directory (.playwright-mcp/)
- [x] Add macOS metadata (.DS_Store, .AppleDouble, .LSOverride, ._*)
- [x] Add IDE/editor files (.idea/, .vscode/, *.swp, *~)
- [x] Add environment/secrets (.env, .env.local, .env.*.local)
- [x] Add log files (*.log)
- [x] Add testing artifacts (*.profraw, lcov.info, tarpaulin-report.html)

### 6. Version Bump
- [x] Bump version from 0.1.0 to 0.1.1 in Cargo.toml/Cargo.lock

### 7. Quality and Validation
- [x] cargo clippy passes clean
- [x] cargo build succeeds
- [x] All 7 unit tests pass

## Related Issues

- Closes #19 - Gemini CLI collector now returns real data via JSONL
- Closes #59 - .gitignore expanded with common artifacts
- Refs #64 - Addresses Gemini CLI / Antigravity portion of IDE/coding-tool support
