# Task List - Table UI Enhancements

## Session Context

- **Date**: 2026-04-15/16
- **Branch**: feature/table-ui-enhancements
- **Objective**: Improve daily/weekly/monthly table rendering for better alignment, readability, per-model breakdown, and provider grouping

## Tasks Completed

### 1. Dynamic Column Widths
- [x] Calculate column widths based on actual data content (model names, token values, costs)
- [x] Remove hardcoded col_date/col_models/col_input/col_output/col_cost constants
- [x] Scan all display rows (provider + model) to find maximum widths

### 2. Shorter Model Names
- [x] Extended shorten_model() to strip `antigravity-` prefix
- [x] Models column width adjusts dynamically to fit longest shortened name

### 3. Provider Grouping with Aggregates
- [x] Extended DailyRow with `model_entries: Vec<ModelEntry>` (provider, model, input, output, cost)
- [x] Modified query_grouped() to GROUP BY period, provider, model and return per-model cost
- [x] Provider rows show aggregate input/output/cost in magenta color
- [x] Model rows show per-model breakdown in dimmed style

### 4. Per-Model Token and Cost Breakdown
- [x] Each model under a provider shows its own input_tokens, output_tokens, cost
- [x] Provider header shows the sum of its models' values
- [x] Period totals remain the sum of all providers

### 5. Dotted Line Separators Between Models
- [x] Added dotted separator (·) between models within the same provider
- [x] Dotted lines span all columns for visual consistency
- [x] Only shown when provider has >1 model

### 6. Date Column Width Fix
- [x] Date column accommodates full YYYY-MM-DD (daily mode)
- [x] Weekly: single-line "2026 W15" format
- [x] Monthly: single-line "2026-04" format
- [x] Removed multi-line date splitting

### 7. Hide Empty-Value Models (--all flag)
- [x] Added `--all` flag to Daily, Weekly, Monthly CLI commands
- [x] Pass show_all parameter through to display::print_daily()
- [x] When --all is NOT set: hide models with zero input AND zero output tokens
- [x] When --all IS set: show all models

### 8. Period Separators
- [x] Solid separator lines between different periods (days/weeks/months)

### 9. Alignment Fix
- [x] Fixed weekly mode alignment issues
- [x] All columns properly aligned across all modes using dynamic width computation

### 10. Build Verification
- [x] cargo build — zero warnings
- [x] cargo clippy — zero warnings
- [x] cargo install — binary installed
- [x] Smoke test: llmusage daily, weekly, monthly with and without --all
